<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeez.zp.platform.mapper.SortSettingMapper">

    <update id="updateRoomCategorySortNo">
        UPDATE room_category
        SET sort_no = #{sortNo},
            updated_by = #{userId},
            updated_at = CURRENT_TIMESTAMP,
            version_no = version_no + 1
        WHERE camp_id = #{campId}
          AND room_category_id = #{roomCategoryId}
          AND status = 1
          AND is_deleted = 0
    </update>

    <update id="updateGoodsSortRemark">
        UPDATE goods_main
        SET remark = CONCAT('sort:', #{sortNo}),
            updated_at = CURRENT_TIMESTAMP
        WHERE camp_id = #{campId}
          AND goods_id = #{goodsId}
          AND status = 'published'
          AND is_deleted = 0
    </update>
</mapper>
